.DS_Store
Thumbs.db
desktop.ini
*.log
.vscode/

# Claude / agent tooling. These live one level up, in the project root
# (C:\Dev\faisalhenar.com), deliberately outside this repo. Listed here only
# as a safety net in case a tool ever recreates them inside the repo.
/CLAUDE.md
/NOTES.md
/skills-lock.json
/.claude/
/.agents/
/.mcp.json

# Same as above, but unanchored: a stray practice/.claude/ turned up once
# (a tool wrote local settings a level too deep). Catch it wherever it lands.
.claude/

# Never commit credentials, wherever they end up.
.github-token
*.token
.env

# Temporary working tools, not part of the production site.
/_working/
